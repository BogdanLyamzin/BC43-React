const listTitle = {
    marginBottom: "15px",
    fontWeight: "bold",
    fontSize: "16px",
    fontFamily: `Arial, "Helvetica Neue", Helvetica, sans-serif`,
}

const list = {
    listStyle: "none",
}

export default {
    listTitle,
    list,
}